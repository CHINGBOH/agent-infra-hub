<!-- AUTO-GENERATED by tools/docs_gen.py — DO NOT EDIT BY HAND. Run `make docs` to regenerate. -->

# agent_kb.py — Raw `--help` Output

_Source: `tools/agent_kb.py` · Regenerated by `make docs` (2026-05-22)_

## Top-level

```
usage: docs_gen.py [-h] [--db DB]
                   {build,search,ask,answer,show,recommend,docs,repl,stats} ...

Build and query the local agent-infra-hub knowledge base.

positional arguments:
  {build,search,ask,answer,show,recommend,docs,repl,stats}
    build               Build the local SQLite/FTS knowledge index
    search              Search indexed chunks
    ask                 Return a structured context pack for a human or agent
                        question
    answer              Generate a DeepSeek-synthesized answer from retrieved
                        context
    show                Show a chunk by id
    recommend           Recommend relevant skills, agents, and source paths
                        for an intent
    docs                List indexed documents
    repl                Start an interactive human/agent query shell
    stats               Show index statistics

options:
  -h, --help            show this help message and exit
  --db DB               SQLite index path
```

## `build`

```
usage: docs_gen.py build [-h] [--root ROOT] [--include-code] [--json]

options:
  -h, --help      show this help message and exit
  --root ROOT     Repository root
  --include-code  Also index common code files
  --json
```

## `search`

```
usage: docs_gen.py search [-h] [--limit LIMIT] [--json] [--category CATEGORY]
                          [--doc-type DOC_TYPE] [--tag TAG] [--include-docs]
                          [--doc-limit DOC_LIMIT] [--doc-chars DOC_CHARS]
                          query

positional arguments:
  query

options:
  -h, --help            show this help message and exit
  --limit LIMIT
  --json
  --category CATEGORY   Filter by top-level category, for example 05-subagents
                        or use-cases
  --doc-type DOC_TYPE   Filter by inferred document type, for example skill,
                        subagent, use_case, catalog
  --tag TAG             Filter by inferred domain tag, for example
                        construction_cost, statistics, governance
  --include-docs        Include reconstructed markdown documents for top hits
  --doc-limit DOC_LIMIT
                        Maximum documents to include when --include-docs is
                        set
  --doc-chars DOC_CHARS
                        Maximum characters per included document
```

## `ask`

```
usage: docs_gen.py ask [-h] [--limit LIMIT] [--json] [--category CATEGORY]
                       [--doc-type DOC_TYPE] [--tag TAG] [--include-docs]
                       [--doc-limit DOC_LIMIT] [--doc-chars DOC_CHARS]
                       question

positional arguments:
  question

options:
  -h, --help            show this help message and exit
  --limit LIMIT
  --json
  --category CATEGORY   Filter by top-level category, for example 05-subagents
                        or use-cases
  --doc-type DOC_TYPE   Filter by inferred document type, for example skill,
                        subagent, use_case, catalog
  --tag TAG             Filter by inferred domain tag, for example
                        construction_cost, statistics, governance
  --include-docs        Include reconstructed markdown documents for top hits
  --doc-limit DOC_LIMIT
                        Maximum documents to include when --include-docs is
                        set
  --doc-chars DOC_CHARS
                        Maximum characters per included document
```

## `answer`

```
usage: docs_gen.py answer [-h] [--limit LIMIT] [--source-limit SOURCE_LIMIT]
                          [--temperature TEMPERATURE]
                          [--max-tokens MAX_TOKENS] [--timeout TIMEOUT]
                          [--json] [--category CATEGORY] [--doc-type DOC_TYPE]
                          [--tag TAG] [--include-docs] [--doc-limit DOC_LIMIT]
                          [--doc-chars DOC_CHARS]
                          question

positional arguments:
  question

options:
  -h, --help            show this help message and exit
  --limit LIMIT
  --source-limit SOURCE_LIMIT
  --temperature TEMPERATURE
  --max-tokens MAX_TOKENS
  --timeout TIMEOUT
  --json
  --category CATEGORY   Filter by top-level category, for example 05-subagents
                        or use-cases
  --doc-type DOC_TYPE   Filter by inferred document type, for example skill,
                        subagent, use_case, catalog
  --tag TAG             Filter by inferred domain tag, for example
                        construction_cost, statistics, governance
  --include-docs        Include reconstructed markdown documents for top hits
  --doc-limit DOC_LIMIT
                        Maximum documents to include when --include-docs is
                        set
  --doc-chars DOC_CHARS
                        Maximum characters per included document
```

## `show`

```
usage: docs_gen.py show [-h] [--json] chunk_id

positional arguments:
  chunk_id

options:
  -h, --help  show this help message and exit
  --json
```

## `recommend`

```
usage: docs_gen.py recommend [-h] [--limit LIMIT] [--json]
                             [--category CATEGORY] [--doc-type DOC_TYPE]
                             [--tag TAG] [--include-docs]
                             [--doc-limit DOC_LIMIT] [--doc-chars DOC_CHARS]
                             intent

positional arguments:
  intent

options:
  -h, --help            show this help message and exit
  --limit LIMIT
  --json
  --category CATEGORY   Filter by top-level category, for example 05-subagents
                        or use-cases
  --doc-type DOC_TYPE   Filter by inferred document type, for example skill,
                        subagent, use_case, catalog
  --tag TAG             Filter by inferred domain tag, for example
                        construction_cost, statistics, governance
  --include-docs        Include reconstructed markdown documents for top hits
  --doc-limit DOC_LIMIT
                        Maximum documents to include when --include-docs is
                        set
  --doc-chars DOC_CHARS
                        Maximum characters per included document
```

## `docs`

```
usage: docs_gen.py docs [-h] [--limit LIMIT] [--json] [--category CATEGORY]
                        [--doc-type DOC_TYPE] [--tag TAG]

options:
  -h, --help           show this help message and exit
  --limit LIMIT
  --json
  --category CATEGORY  Filter by top-level category, for example 05-subagents
                       or use-cases
  --doc-type DOC_TYPE  Filter by inferred document type, for example skill,
                       subagent, use_case, catalog
  --tag TAG            Filter by inferred domain tag, for example
                       construction_cost, statistics, governance
```

## `repl`

```
usage: docs_gen.py repl [-h]

options:
  -h, --help  show this help message and exit
```

## `stats`

```
usage: docs_gen.py stats [-h] [--json]

options:
  -h, --help  show this help message and exit
  --json
```
